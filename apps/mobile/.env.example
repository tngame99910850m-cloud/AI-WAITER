# Base URL of the AI Waiter API. On a device/emulator, localhost is the device
# itself — use your machine's LAN IP (e.g. http://192.168.1.20:4000).
EXPO_PUBLIC_API_URL=http://localhost:4000
# Client API key (safe to ship; the server holds the real secrets).
EXPO_PUBLIC_API_KEY=dev-client-key
